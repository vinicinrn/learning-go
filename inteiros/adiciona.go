package inteiros

//leitura: recebe 2 param inteiros e retorna um inteiro
func Adiciona(x, y int) int { //DECLARACAO DE RETORNO: geralmente usado quando o significado do resultado nao está claro no contexto, nesse caso, não precisa.
	return x + y
}
